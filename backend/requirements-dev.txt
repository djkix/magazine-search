# Dépendances de test uniquement. Ne pas installer dans l'image de production.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
# Requis par starlette.testclient.TestClient, utilisé pour les tests
# d'autorisation (aucun serveur n'est démarré : les appels passent en mémoire).
httpx==0.27.2
